import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.blecap.app',
  appName: 'ble-cap',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
